<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".MenuFragment"
    android:background="@color/black">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbarMenu"
        android:layout_width="match_parent"
        android:layout_height="?actionBarSize"
        android:background="@color/black"
        app:title="Spotify downloader"
        app:titleTextColor="@color/green"
        />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/toolbarMenu"
        android:gravity="center"
        android:orientation="vertical">


        <ImageView
            android:id="@+id/imageViewLogo"
            android:layout_width="300dp"
            android:layout_height="300dp"
            android:layout_weight="1"
            tools:src="@drawable/menu_logo"
            android:contentDescription="logo_spotify_downloader"/>

        <Button
            android:id="@+id/buttonDownloadMenu"
            android:layout_width="250dp"
            android:layout_height="60dp"
            android:text="Download songs"
            android:backgroundTint="@color/green"
            android:textSize="20dp"
            android:textColor="@color/white"
            android:layout_marginVertical="10dp"
            />

        <Button
            android:id="@+id/buttonDownloadHistory"
            android:layout_width="250dp"
            android:layout_height="60dp"
            android:text="History"
            android:backgroundTint="@color/green"
            android:textSize="20dp"
            android:textColor="@color/white"
            android:layout_marginVertical="10dp"
            />



    </LinearLayout>



</RelativeLayout>